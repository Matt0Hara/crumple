#!/usr/bin/env ruby
require "fileutils"
require "crumple"

options = {}

opt_parser = OptionParser.new do |opt|
  opt.banner = "Hey guys!"
  opt.separator  ""
  opt.separator  "Commands"
  opt.separator  "     Simply input a filename to export it!"
  opt.separator  ""
  opt.separator  "Options"


  opt.on("FILE","--dump","move a file, dude") do
    # dump(ARGV[0])
    puts "hi"
  end

  opt.on("-s","--dir","set dump directory") do

  end

  opt.on("-h","--help","help") do
    puts opt_parser
  end
end

opt_parser.parse!


# if ARGV.length == 1
#   if ARGV[0]
#     input = ARGV[0]
#     dest = "/Users/matthewohara/dump/#{input}"
#     FileUtils.mv(input, dest)
# else
#   puts "Crumple only accepts exactly one argument right now. Who do you think you are?"
# end
